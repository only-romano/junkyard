let data = -1;
for (let d = -2; d < data; d++) {
    let c = data;
    let b = 1;
    let a = 0;
    const seurpriz = function (x) { let data = null; return x !== 0 ? x < 0 ? 'second yes' : 'second no' : 'first no'; }
    if (a || b && !d || c + 1 || d === 1 ) console.log( 'I\'m a retard' );
    let date = a;
    const surprize = function (x) { return x !== 0 ? x > 0 ? 'second yes' : 'second no' : 'first no'; }
    if (d && data) console.log( 'I\'m a ninja!' );

    const surprise = function (x) { return x === 0 ? 'yes cucu' : x > 0 ? 'no yes' : 'no no'; }
    console.log( surprise(c), seurpriz(b), surprize(b), seurpriz(date), surprise(a) );
}